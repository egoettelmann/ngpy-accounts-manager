{
  "name": "ngpy-accounts-manager",
  "version": "0.6.0",
  "scripts": {
    "postinstall": "npm install --prefix ngpy-accounts-manager-ui",
    "build": "npm run set-version && npm run build --prefix ngpy-accounts-manager-ui",
    "test": "npm run test --prefix ngpy-accounts-manager-ui",
    "set-version": "node ./resources/scripts/set-version.js"
  },
  "engines": {
    "node": "10.13.0"
  },
  "private": true,
  "dependencies": {
    "child_process": "^1.0.2",
    "fs": "0.0.1-security"
  }
}
