{
  "name": "ngpy-accounts-manager",
  "version": "0.4.1",
  "scripts": {
    "postinstall": "npm run set-version && npm run build-front",
    "build-front": "npm install --prefix frontend && npm run build --prefix frontend",
    "set-version": "node ./resources/scripts/set-version.js"
  },
  "engines": {
    "node": "10.13.0"
  },
  "dependencies": {
    "child_process": "^1.0.2",
    "fs": "0.0.1-security"
  }
}
